spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_rpg
    username: rpguser
    password: rpgpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true